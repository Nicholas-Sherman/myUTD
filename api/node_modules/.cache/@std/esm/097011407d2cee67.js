"main";let PythonShell;_7d2‍.w('python-shell',[["PythonShell",function(v){PythonShell=v}]]);let axios;_7d2‍.w('axios',[["default",function(v){axios=v}]]);

var express =require("express");
var router= express.Router();

let res = await axios.get('http://localhost:9000/name');
let data = res.data;
console.log(data);

  PythonShell.run('RMPClass.py', {args: ['hey']}, function (err, results) {
    if (err) throw err;
    // results is an array consisting of messages collected during execution
    console.log( results);
  })
