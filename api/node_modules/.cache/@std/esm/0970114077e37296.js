"main";let PythonShell;_77e‍.w('python-shell',[["PythonShell",function(v){PythonShell=v}]]);
var router= express.Router();

router.post('/', (req, res) => {
  var postData  = req.body.name;
   var nameS = postData.split(" ");
   firstN = nameS[0]
   lastN = nameS[1]
   console.log(firstN)
   console.log(lastN)

  // PythonShell.run('RMPClass.py', {args: ['hey']}, function (err, results) {
  //   if (err) throw err;
  //   // results is an array consisting of messages collected during execution
  //   console.log( results);
  // })
})